# Copy to keystore.properties (gitignored) for local release signing.
# CI uses GitHub Actions secrets instead (see .github/workflows).
storeFile=yamtrack-release.jks
storePassword=CHANGE_ME
keyAlias=yamtrack
keyPassword=CHANGE_ME
