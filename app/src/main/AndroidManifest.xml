<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".YamtrackApplication"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yamtrack"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Yamtrack.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom-scheme deep links: yamtrack://media/<type>/<src>/<id>[/<season>[/<ep>]]
                 and yamtrack://lists/<id>. Works from any source (QR code,
                 shortcut, server email, etc.) regardless of which host the
                 user's Yamtrack server is on. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yamtrack" />
            </intent-filter>

            <!-- https deep links into the default Yamtrack instance. Users
                 with a different host can add their own <data> entries. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="yamtrack.fuzzygrim.com" />
                <data android:pathPrefix="/media" />
                <data android:pathPrefix="/lists" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Yamtrack.NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Yamtrack.NoActionBar" />

        <activity
            android:name=".ui.details.MediaDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.Yamtrack.NoActionBar"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.episodes.EpisodesActivity"
            android:exported="false"
            android:theme="@style/Theme.Yamtrack.NoActionBar"
            android:parentActivityName=".ui.details.MediaDetailsActivity" />

        <activity
            android:name=".ui.lists.ListsActivity"
            android:exported="false"
            android:theme="@style/Theme.Yamtrack.NoActionBar"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.lists.ListItemsActivity"
            android:exported="false"
            android:theme="@style/Theme.Yamtrack.NoActionBar"
            android:parentActivityName=".ui.lists.ListsActivity" />

        <!-- Calendar widget -->
        <receiver
            android:name=".widget.CalendarWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_info" />
        </receiver>

        <service
            android:name=".widget.CalendarWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

    </application>

</manifest>
